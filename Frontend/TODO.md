# Task: Organize Types for Backends

## Overview
- Ensure types have backend individual types folders and their backends.
- Remove all type declarations from API files.
- Export types in types/api.ts for easy import.

## Steps
- [x] Create types files in Frontend/src/types/pythonbackendapi/ by extracting from API files.
- [ ] Remove type declarations from pythonbackendapi API files and add imports.
- [ ] Remove type declarations from javabackendapi API files and add imports.
- [ ] Remove type declarations from nodejsbackendapi API files and add imports.
- [ ] Update Frontend/src/types/api.ts to export all backend types.
- [ ] Verify all changes and ensure no duplicates.
